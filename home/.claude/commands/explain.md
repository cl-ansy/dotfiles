Explain the following in plain English: $ARGUMENTS

If no target is given, explain the current file or most recently discussed code.

Structure your explanation as:

### What it does
One paragraph. What problem does this code solve? What does it produce?

### How it works
Walk through the logic step by step. Use concrete examples with real values where helpful. Avoid jargon — if you must use a technical term, define it inline.

### Key decisions
What non-obvious choices were made in this implementation? Why might the author have done it this way?

### Gotchas
What would trip up someone reading or modifying this code? Side effects, implicit assumptions, ordering dependencies, surprising behavior.

Calibrate depth to the complexity of the code. A 5-line utility needs a short answer. A complex algorithm or architectural pattern warrants more detail.
