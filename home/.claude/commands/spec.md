Turn the following idea into a concrete implementation plan: $ARGUMENTS

Explore the codebase to understand the relevant context before writing anything.

Output a spec with these sections:

### Problem
One paragraph: what problem does this solve and for whom?

### Approach
The implementation strategy. If multiple approaches exist, list them with trade-offs, then recommend one.

### Files to Change
List each file that needs to be created or modified, with a one-line description of what changes.

### Implementation Steps
Numbered, ordered steps. Each step should be independently completable and testable. Flag which steps can be parallelized.

### Edge Cases & Risks
What could go wrong? What assumptions are we making? What's out of scope?

### Open Questions
Anything that needs a decision before implementation starts.

Do not write code yet — this is a plan for approval.
